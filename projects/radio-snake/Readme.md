This is a cut down version of https://github.com/jpmonettas/radio-snake which removes the 
HackRF One interface part which requires libhackrf to be installed and a device to use.

Start with :

```bash
$ clj -A:dev:1.12-storm:fs-async-flow-plugin
```
